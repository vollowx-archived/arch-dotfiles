#--------#
# global #
#--------#
include @sysconfdir@/sway/config.d/*

#------------#
# appearence #
#------------#
include catppuccin-mocha

# target                 title     bg    text   indicator  border
client.focused           $sky      $base $text  $rosewater $sky
client.focused_inactive  $mauve    $base $text  $rosewater $overlay2
client.unfocused         $mauve    $base $text  $rosewater $overlay0
client.urgent            $red      $base $red   $overlay0  $red
client.placeholder       $overlay0 $base $text  $overlay0  $overlay0
client.background        $base

# smart_borders on
default_border pixel 4
default_floating_border pixel 4
gaps inner 8
gaps outer 2

# Electron apps have some problems with this, so scale font
output eDP-1 scale 1
output * bg /home/pancak/.local/share/backgrounds/wallhaven-dgv23j.png fill
# output * bg /home/pancak/.local/share/backgrounds/Street.jpg fill

# font JetBrainsMono Nerd Font 10

# change GTK themes in gnome-tweaks


#-------------#
# startup env #
#-------------#
exec systemctl --user import-environment DISPLAY WAYLAND_DISPLAY SWAYSOCK
exec hash dbus-update-activation-environment 2>/dev/null && dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY SWAYSOCK XDG_CURRENT_DESKTOP
xwayland enabled


#-----------#
# autostart #
#-----------#
exec --no-startup-id fcitx5 -d --replace
exec wl-paste -t text --watch clipman store
exec mako


#-----------#
# variables #
#-----------#
set $mod Mod4

set $left h
set $down j
set $up k
set $right l

set $term kitty
set $menu wofi --show run


#---------#
# devices #
#---------#
input type:keyboard {
  xkb_layout us
  repeat_delay 300
  repeat_rate 30
}

input type:touchpad {
  dwt enabled
  accel_profile adaptive
  pointer_accel 0.3
  tap enabled
  natural_scroll enabled
  middle_emulation enabled
}

seat * hide_cursor 8000

bindswitch --reload --locked lid:on output eDP-1 disable
bindswitch --reload --locked lid:off output eDP-1 enable


#----------#
# swayidle #
#----------#
exec swayidle -w \
  timeout 300 'swaylock -f' \
  timeout 600 'swaymsg "output * dpms off"' resume 'swaymsg "output * dpms on"' \
  before-sleep 'swaylock -f'


#--------------#
# window rules #
#--------------#
for_window [app_id="termfloat"]      floating enable
for_window [app_id="termfloat"]      resize set height 480
for_window [app_id="termfloat"]      resize set width 800

for_window [app_id="mpv"]            floating enable 
for_window [app_id="mpv"]            resize set height 480
for_window [app_id="mpv"]            resize set width 800


#-------------#
# keybindings #
#-------------#
bindsym $mod+Return exec $term
bindsym $mod+Shift+Return exec $term --class="termfloat"
bindsym $mod+space exec pkill wofi || wofi --show run
bindsym $mod+q kill
bindsym $mod+Shift+q exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -b 'Yes, exit sway' 'swaymsg exit'
bindsym $mod+Shift+r reload
bindsym $mod+x exec swaylock
bindsym $mod+Escape exec wlogout
bindsym $mod+Shift+space floating toggle
bindsym $mod+f fullscreen
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
bindsym $mod+b splith
bindsym $mod+v splitv
bindsym $mod+Shift+v exec clipman pick -t wofi
bindsym $mod+Shift+c exec hyprpicker -n -a
bindsym $mod+Shift+o exec killall -SIGUSR1 waybar
bindsym --locked $mod+d exec sh -c "notify-send '󰪑 Do Not Disturb' 'Turning on Do Not Disturb Mode' && sleep 2 && makoctl set-mode do-not-disturb"
bindsym --locked $mod+Shift+d exec sh -c "makoctl set-mode default && notify-send '󰂜 Do Not Disturb' 'Do Not Disturb Mode disabled'"

bindsym $mod+a focus parent
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show
floating_modifier $mod normal

bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

bindsym $mod+Shift+1 move container to workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10

mode "resize" {
  bindsym $left resize shrink width 10px
  bindsym $down resize grow height 10px
  bindsym $up resize shrink height 10px
  bindsym $right resize grow width 10px
  bindsym Return mode "default"
  bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"


#---------------------#
# special keybindings #
#---------------------#
bindsym XF86AudioRaiseVolume exec pamixer -i 2 && notify-send "󰕾 Volume" "Volume: $(pamixer --get-volume)%" --hint="int:value:$(pamixer --get-volume)"
bindsym XF86AudioLowerVolume exec pamixer -d 2 && notify-send "󰕾 Volume" "Volume: $(pamixer --get-volume)%" --hint="int:value:$(pamixer --get-volume)"
bindsym XF86AudioMute exec pamixer -t && notify-send "󰕾 Volume" "Volume: $(pamixer --get-volume)%" --hint="int:value:$(pamixer --get-volume)"

# This needs `sudo chmod +s /usr/bin/light`
bindsym XF86MonBrightnessDown exec light -U 5 && notify-send "󰃠 Light" "Brightness: $(light)%" --hint="int:value:$(light)"
bindsym XF86MonBrightnessUp exec light -A 5 && notify-send "󰃠 Light" "Brightness: $(light)%" --hint="int:value:$(light)"

bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

bindsym Print exec grimblast copy area
bindsym $mod+Ctrl+Print exec grimblast copysave screen $(xdg-user-dir PICTURES)/Screenshots/$(date +%Y-%m-%d_%H-%M-%S).png
bindsym $mod+Shift+Print exec grimblast copysave area $(xdg-user-dir PICTURES)/Screenshots/$(date +%Y-%m-%d_%H-%M-%S).png 


#----------------------#
# waybar & keybindings #
#----------------------#
bar {
  swaybar_command waybar
}
